# dependencies
/node_modules
/.pnp
/prisma
.pnp.js

# misc
.DS_Store
*.pem
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local
.env.production

# typescript
*.tsbuildinfo

# anchor / solana
/target
/programs/**/target
.anchor
.surfpool
**/*.rs.bk

# indexer (separate Cargo workspace — kept isolated from the Anchor
# program's on-chain dependency tree; see indexer/README.md)
/indexer/target
/indexer/decoder/target
/indexer/dlmm-bridge/node_modules
/indexer/dlmm-bridge/dist

# git worktrees
/.worktrees

# impeccable design skill — local live-mode session state and hook cache,
# not project config (that's just /.impeccable/live/config.json, tracked)
/.impeccable/live/sessions
/.impeccable/live/accept-receipts
/.impeccable/hook.cache.json
